# NanoAgent runtime dependencies. Install this together with
# third_party/verl/requirements.txt through scripts/setup_nanoagent_env.sh.
# air-node-02 has an NVIDIA 555 driver; vLLM 0.8.5 is the Qwen3-compatible
# rollout backend used here and requires the Torch 2.6 CUDA 12.4 wheel stack.
--extra-index-url https://download.pytorch.org/whl/cu124

torch==2.6.0+cu124
torchvision==0.21.0+cu124
torchaudio==2.6.0+cu124
torchdata==0.8.0

numpy>=2.0.0,<2.3
pyarrow>=19.0.0
PyYAML>=6.0.0
safetensors>=0.4.0
tokenizers>=0.20.0
# Transformers 5.10 imports FP8 dtypes unavailable in the CUDA 12.1 PyTorch
# stack at module import time; 5.5.3 supports Qwen SFT without that requirement.
transformers==5.5.3
Pillow>=10.0.0
matplotlib>=3.8.0
flash-attn==2.7.4.post1 --no-deps
# Verl's async tool-agent loop needs a vLLM backend; pip resolves its
# transitive runtime packages without a separate environment.
vllm==0.8.5.post1
