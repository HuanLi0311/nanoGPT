# NanoAgent runtime dependencies. Install this together with
# third_party/verl/requirements.txt through scripts/setup_nanoagent_env.sh.
# air-node-03 has an NVIDIA 555 driver (CUDA 12.5); the CUDA 12.1 wheels below
# are driver-compatible and avoid the CUDA 13-only veRL uv environment.
--extra-index-url https://download.pytorch.org/whl/cu121

torch==2.3.1+cu121
torchvision==0.18.1+cu121
torchaudio==2.3.1+cu121
torchdata==0.8.0
tensordict==0.8.3

numpy>=2.0.0
pyarrow>=19.0.0
PyYAML>=6.0.0
safetensors>=0.4.0
tokenizers>=0.20.0
transformers>=5.5.3,!=5.6.0,<5.11
Pillow>=10.0.0
matplotlib>=3.8.0
