# NanoAgent runtime dependencies. Install this together with
# third_party/verl/requirements.txt through scripts/setup_nanoagent_env.sh.
# air-node-03 has an NVIDIA 555 driver (CUDA 12.5); the CUDA 12.1 wheels below
# are driver-compatible, satisfy Transformers 5's PyTorch >=2.4 runtime floor,
# and avoid the CUDA 13-only veRL uv environment.
--extra-index-url https://download.pytorch.org/whl/cu121

torch==2.4.1+cu121
torchvision==0.19.1+cu121
torchaudio==2.4.1+cu121
torchdata==0.8.0

numpy>=2.0.0
pyarrow>=19.0.0
PyYAML>=6.0.0
safetensors>=0.4.0
tokenizers>=0.20.0
# Transformers 5.10 imports FP8 dtypes unavailable in the CUDA 12.1 PyTorch
# stack at module import time; 5.5.3 supports Qwen SFT without that requirement.
transformers==5.5.3
Pillow>=10.0.0
matplotlib>=3.8.0
flash-attn==2.8.3.post1 --no-deps