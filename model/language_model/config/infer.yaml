inference:
  max_new_tokens: 40
  temperature: 1.0
  top_k: 1
  seed: 7

data:
  tokenizer_dir: model/language_model/data/encode/pretrain
  tokenizer_prefix: byte_bpe

checkpoint:
  path: model/language_model/checkpoints/best.safetensors
